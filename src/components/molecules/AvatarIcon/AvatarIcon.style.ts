import { IconButton as MuiIconButton, Menu as MuiMenu } from "@mui/material";
import styled from "styled-components";

export const Menu = styled(MuiMenu)`
  margin-top: 45px;
`;

export const IconButton = styled(MuiIconButton)`
  padding: 0px;
`;
